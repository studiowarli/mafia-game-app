# Mafia: The Game Web App

## Setup
1. **Install Python 3.9+** if not already installed.
2. **Clone the repository** or create a new directory with the provided files.
3. **Install dependencies**:
   ```bash
   pip install -r requirements.txt